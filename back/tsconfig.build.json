{
  // The build ships the server, not the suite that exercises it. `tsconfig.json`
  // still covers the tests so `npm run typecheck` type-checks them too.
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "build", "tests", "vitest.config.ts"]
}
